1️⃣ What is the difference between var, let, and const?

ans: the difference between var let const let, cons is new version it is fimiliar to ES6 name and var is old version it used before published ES6
and var is global scope it can reassign from any where in js. and let is block scope it can reassign but cant redeclare it work in function and const is also block but it can not use function outside it can not redeclare it can reassign .


2️⃣ What is the spread operator (...)?

ans: spread operator er kaj holo object ba array k onno akta array er moddhe choriye ba bisiye dewar jonno jata onno akta rakha jai.


3️⃣ What is the difference between map(), filter(), and forEach()?

ans: the difference between map(), filter() forEach() these are function method but its works diferently map() is work for get make a new array and filter() is work if i want something spicifically then it work conditionally make new array as you want. shortly it work conditionally and foreach() use for on any object or array looping it work for loop oparating but cant make new array only for looping.

4️⃣ What is an arrow function?
 
 ans: arrow is a ES6 version it is use as shortly function it published after 2015 but is easear to use and car write single and double line also.

 5️⃣ What are template literals?

 ans: template literals is the most useful system it can use for write multiline string it can contains varible genelly use `` backtiqe for templeting ${} this way can write variable .